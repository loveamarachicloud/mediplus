name: Docker CI Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  docker_build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t mediplus .
        